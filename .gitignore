node_modules/
dist/
.DS_Store
*.log
.env
.env.local
*.js.map

# Secrets — NEVER commit service account keys
backend/serviceAccountKey.json

# Firebase deploy cache
.firebase/

# Claude Code — เครื่อง local เท่านั้น (machine-local permissions)
.claude/settings.local.json
